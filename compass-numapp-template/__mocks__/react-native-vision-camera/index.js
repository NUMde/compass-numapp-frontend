export const Camera = {
  render: () => {
    return null;
  },
  requestCameraPermission: async () => {
    return true;
  },
};

export const useCameraDevices = () => {
  return { back: null };
};
export const useFrameProcessor = () => {};
